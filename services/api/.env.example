# ----------------------------
# Database (Postgres)
# ----------------------------
# Async SQLAlchemy URL for SQLModel + asyncpg
# Replace user/password/host/port/db as needed.
DATABASE_URL=postgresql+asyncpg://novaflow:CHANGEME_PASSWORD@localhost:5433/novaflow

# ----------------------------
# AWS / Bedrock
# ----------------------------
# AWS CLI profile name (SSO or static credentials profile)
AWS_PROFILE=novaflow

# Region must match where your Bedrock access/model is enabled
AWS_REGION=eu-north-1
AWS_DEFAULT_REGION=eu-north-1
BEDROCK_REGION=eu-north-1

# (Optional, helpful for SSO setups)
AWS_SDK_LOAD_CONFIG=1

# ----------------------------
# Provider selection
# ----------------------------
# bedrock: real AWS Bedrock
# mock: no AWS needed
NOVA_PROVIDER=bedrock

# ----------------------------
# Bedrock Model IDs
# ----------------------------
# Embeddings model
NOVA_EMBED_MODEL_ID=amazon.titan-embed-text-v2:0

# Planner/chat model
# NOTE: Some Nova models require an Inference Profile (not on-demand).
NOVA_LITE_MODEL_ID=eu.amazon.nova-2-lite-v1:0

# Optional: inference profile ARN/ID for Nova if required by your account/region.
# NOVA_INFERENCE_PROFILE_ID=arn:aws:bedrock:eu-north-1:123456789012:inference-profile/your-profile-id

# ----------------------------
# UI Automation / Starting URL policy
# ----------------------------
# demo       -> always uses DEMO_STARTING_URL (safe, ignores task URLs)
# plan       -> uses plan.starting_url only if host is in ALLOWED_STARTING_HOSTS
# any_public -> uses any public http/https host (blocks localhost/private IPs in server logic)
STARTING_URL_MODE=plan

# Comma-separated allowlist for STARTING_URL_MODE=plan
ALLOWED_STARTING_HOSTS=the-internet.herokuapp.com

# Demo fallback URL
DEMO_STARTING_URL=https://the-internet.herokuapp.com/

# ----------------------------
# Playwright
# ----------------------------
# Set false for visible browser during debugging
PLAYWRIGHT_HEADLESS=true

# ----------------------------
# CORS
# ----------------------------
# Comma-separated list of allowed frontend origins.
CORS_ORIGINS=http://localhost:3000,https://yourapp.vercel.app
